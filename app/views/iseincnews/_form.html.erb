<%= form_for @iseincnews, html: { multipart: true } do |f| %> 
  <% if @iseincnews.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@iseincnews.errors.count, "error") %> prohibited this iseincnews from being saved:</h2>

      <ul>
      <% @iseincnews.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
